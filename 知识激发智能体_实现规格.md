# 知识激发智能体实现规格

> 面向对象：实现者  
> MVP 场景：面向学习、技术探索、问题分析和协同头脑风暴，生成高信息密度、可追溯、可行动的回答  
> 技术栈：Python 3.11、LangGraph、Pydantic、SQLite、NumPy

## 0. 产品定义

知识激发智能体通过澄清目标、调用可靠资料、生成竞争性解释、搜索反例并与用户迭代，减少大模型回答中的通识化、低信息量和过早收敛，帮助用户获得更具体、更有根据、更能改变下一步行动的内容。

“知识激发”包含三个彼此关联但目标不同的过程：

1. **模型知识激发**：通过问题分解、资料检索、角色视角、对比、反例和约束，使模型避开高概率但平庸的回答。
2. **用户认知激发**：通过追问例子、判断依据和认知冲突，帮助用户暴露已有理解、盲点与真实需求。
3. **协同推演**：用户与模型共同生成、批判和组合候选方案，最终形成结论、学习路径或可执行产物。

模型参数中的知识不能被视为“一手资料”。一手性必须来自用户提供的原始材料、官方文档、论文、源代码、实验记录等可定位来源；无来源的模型知识可以用于提出候选，但必须标明其不确定性。

## 1. 目标与范围

### 1.1 MVP 目标

| 目标 | 验收方式 |
|---|---|
| 提升回答的信息密度 | 输出中的核心内容能够改变理解、选择或下一步行动，避免重复题意和背景常识 |
| 优先使用高价值来源 | 关键事实尽量关联官方文档、论文、源代码、实验或用户原始材料 |
| 避免单一路径过早收敛 | 在复杂问题上保留多个有实质差异的候选解释或方案，再按标准筛选 |
| 暴露假设和适用边界 | 重要结论明确前提、反例、未知项和失效条件 |
| 促进学习与共同推理 | 输出既给结论，也帮助用户形成可检验的理解、练习或决策依据 |
| 控制交互成本 | 只有问题会显著改变回答时才追问；用户可随时要求直接作答或停止 |
| 支持回放与评测 | 保存来源、候选、评分、修订和最终结论，允许比较普通直答基线 |

### 1.2 典型场景

场景不作为路由的封闭枚举，只用于说明能力边界：

- **巩固知识**：复习 Python，发现语法会用但对迭代器、闭包或并发模型理解不牢，并生成针对性练习。
- **学习工具**：学习 Unity，通过一个可运行的小项目串联场景、组件、生命周期、物理与脚本，而不是罗列 API。
- **理解新架构**：学习 LangChain 或 LangGraph，从核心抽象、状态流转、持久化和适用边界建立心智模型，并以官方文档和源码为依据。
- **解决技术问题**：比较多个实现方案，定位真正分歧、隐含成本和可能失败的条件。
- **研究与写作**：从论文、实验和反例中提炼论点，区分已有证据、推断和待验证假设。
- **开放式探索**：围绕产品、研究问题或创作目标发散候选，再通过新颖性、相关性和可行性筛选组合。
- **经验外化**：复盘一次学习、实验或决策，将隐性判断整理为有来源、有边界的 Claim。

### 1.3 非目标

- 不声称能访问或完整枚举模型内部“知识空间”。系统优化的是可观察输出，而不是模型参数本身。
- 不把模型推测、角色扮演或自述推理过程当作事实证据。
- 不把“更长”“术语更多”当作信息量更高；信息价值取决于相关性、区分度、证据和行动影响。
- 不为发散而无限生成候选，也不让多个角色进行无目标的长篇辩论。
- 不在缺少工具或来源时伪装成基于一手资料；此时必须标注“模型已有知识，未核验”。
- 不要求所有任务都经历完整流程。简单事实题、明确代码修改和低风险问题应快速作答。

## 2. 设计原则

1. **先定义价值，再生成内容。** 每个任务先明确产物、受众、约束和“什么会改变下一步行动”。
2. **来源分级。** 原始资料与可定位证据优先于二手总结，二手总结优先于未经核验的模型记忆。
3. **生成与评判分离。** 先产生实质不同的候选，再按证据、相关性、新颖性、可行性和风险筛选。
4. **具体性约束。** 优先要求机制、例子、边界、对比、失败模式和可执行步骤，压制空泛概括。
5. **用户与模型双向激发。** 既追问用户已有理解，也允许模型主动提出用户尚未考虑的假设。
6. **推断可见，私有推理不可依赖。** 输出简洁的证据和推断关系，不保存或要求模型披露隐藏思维链。
7. **计算可复现。** EIG、评分聚合、阈值与停止条件由代码执行；LLM 负责生成和语义判断。
8. **按任务缩放。** 简单问题直接回答；只有高不确定、高价值或开放式问题才进入深度流程。
9. **允许不知道。** 无资料、证据冲突或不确定时明确说明，并给出最低成本的验证方法。
10. **目标隔离。** 澄清、收敛和发散使用不同目标，不能把 EIG 与多样性简单相加。

## 3. MVP 工作流

```text
用户请求
  │
  ▼
task_frame：识别目标、已有知识、约束、期望产物和时效性
  │
  ▼
depth_router
  ├── fast：明确、低风险、无需多方案 ───────────────► answer
  └── deep
       │
       ▼
clarify：只询问会改变答案的缺失信息
       │
       ▼
source：读取用户材料；需要时检索官方文档、论文或源码
       │
       ▼
diverge：生成 3～5 个实质不同的解释、路径或方案
       │
       ▼
challenge：为候选寻找反例、边界、依赖和失败模式
       │
       ▼
synthesize：依据价值标准筛选、组合并形成答案
       │
       ├── 用户继续追问 / 新证据 ───────────────► task_frame
       ▼
learn_check：按需让用户解释、预测、练习或实施
       │
       ▼
finalize：输出结论、依据、未知项和下一步
```

### 3.1 深度路由

满足任一条件时进入 deep：

- 用户要求深入学习、比较、设计、研究、诊断或头脑风暴；
- 问题存在多个合理答案，选择取决于约束；
- 结论时效性强，需要最新资料；
- 错误答案代价较高；
- 用户提供了材料，希望抽取隐含信息或形成新结论；
- 初始回答的信息价值评分低于阈值。

否则使用 fast，直接给出紧凑答案。禁止为了展示流程而过度追问。

### 3.2 澄清策略

开放式问题用于理解目标和激活用户认知；离散问题用于辨析明确的竞争假设。只有后一类问题使用 EIG。

通用澄清维度：

- **目标**：你最终想能解释、能实现、能比较，还是能独立解决问题？
- **当前状态**：已经会什么，具体卡在哪里？
- **约束**：时间、技术版本、运行环境、资料范围和风险容忍度是什么？
- **证据**：有哪些代码、报错、文档段落、实验结果或反例？
- **验收**：怎样才算这次问答真正完成？

学习 Python 时可以问“你能读懂生成器代码，但不会自己写，还是连惰性求值的行为也无法预测？”；学习 LangGraph 时可以问“你的目标是理解状态图，还是实现可中断、可恢复的生产工作流？”。这类问题比泛问“你想学到什么程度”更能改变后续内容。

### 3.3 高信息量生成约束

deep 模式至少使用其中三类约束，但不能机械堆叠：

- **机制约束**：说明它为什么工作，而不只描述是什么。
- **对比约束**：与最相近概念或替代方案比较，指出决定性差异。
- **边界约束**：说明何时不适用、何时会失败。
- **证据约束**：关键事实绑定来源或标明未核验。
- **实例约束**：给一个最小可运行、可计算或可检查的例子。
- **反事实约束**：改变一个关键条件，检查结论是否仍成立。
- **行动约束**：给出下一步可执行行为及成功信号。
- **压缩约束**：删除不改变理解、选择或行动的段落。

约束的作用是改变采样方向和评判标准，不是让回答固定成同一种模板。

### 3.4 协同头脑风暴

发散阶段应生成不同“机制路径”，而不是同义改写。可从以下轴中按任务选择两到三个：时间尺度、实现层级、理论视角、用户角色、资源约束、风险偏好、相邻领域类比。

每个候选包含：核心想法、与其他候选的关键差异、依据、最大风险、最低成本验证。随后由用户或评分器决定保留、组合、修改或淘汰。模型不能用多数投票代替论证。

## 4. 数据模型

### 4.1 AgentState

```python
class AgentState(TypedDict):
    session_id: str
    phase: Literal[
        "task_frame", "clarify", "source", "diverge", "challenge", "synthesize",
        "learn_check", "finalize", "completed"
    ]
    topic: str
    task_type: Literal[
        "learn", "solve", "compare", "design", "research", "brainstorm", "externalize"
    ]
    depth: Literal["fast", "deep"]
    desired_output: str
    user_context: dict[str, Any]
    value_criteria: list[ValueCriterion]
    evidence_items: list[EvidenceItem]
    hypothesis_sets: list[HypothesisSet]
    candidates: list[Candidate]
    claims: list[Claim]
    qa_history: list[Turn]
    pending_question: Question | None
    pending_challenge: Challenge | None
    entropy_trace: list[EntropyRecord]
    budget: Budget
    proposal_attempts: int
    stop_reason: str | None
```

`session_id` 同时作为 LangGraph `thread_id`。恢复会话时必须使用相同 ID。

### 4.2 EvidenceItem

`EvidenceItem` 统一表示用户材料、外部资料和实际观察，不能只覆盖个人经历。

```json
{
  "id": "ev_001",
  "kind": "experiment_observation",
  "context": "对照组与实验组使用相同随机种子重新训练",
  "observed_at_time": ["验证集损失仍相差约 12%"],
  "later_learned": ["数据划分脚本会按文件名排序"],
  "current_interpretation": ["原结果可能受到数据泄漏影响"],
  "source_spans": [
    {
      "turn_id": "turn_03",
      "quote": "我当时只看到换了种子以后差距还在，大约百分之十二",
      "relation": "supports"
    }
  ],
  "source_tier": "primary",
  "locator": "user:turn_03",
  "status": "user_confirmed"
}
```

`kind` 可取：

- `personal_event`
- `experiment_observation`
- `paper_passage`
- `course_example`
- `code_result`
- `advisor_feedback`
- `official_documentation`
- `source_code`
- `research_paper`
- `runtime_observation`
- `model_knowledge`

来源等级：

- `primary`：官方文档、标准、论文原文、源代码、实际运行结果、用户提供的原始材料；
- `secondary`：教程、综述、社区解释；
- `model_only`：模型已有知识但未外部核验。

`primary` 表示更接近原始来源，不表示内容必然正确。所有外部来源保存 URL、文件位置、版本或段落定位；`model_only` 不得伪装成引用。经验复盘中，`observed_at_time`、`later_learned` 和 `current_interpretation` 仍不得合并。

### 4.3 Candidate 与价值标准

```python
class Candidate(BaseModel):
    id: str
    text: str
    mechanism: str
    differentiator: str
    evidence_ids: list[str]
    assumptions: list[str]
    failure_modes: list[str]
    verification: str
    scores: dict[str, float]
    status: Literal["generated", "kept", "combined", "rejected"]

class ValueCriterion(BaseModel):
    name: Literal[
        "relevance", "specificity", "evidence", "novelty", "actionability", "robustness"
    ]
    weight: float
```

评分用于排序和诊断，不能冒充客观真值。不同任务采用不同权重：学习任务提高“机制、可检验性”的权重；技术选型提高“证据、鲁棒性、可执行性”的权重；头脑风暴先强调差异性，收敛时再强调相关性和可行性。

### 4.4 HypothesisSet

EIG 只在一个冻结的 `HypothesisSet` 内计算。集合中的假设必须描述同一个潜变量，并尽量互斥、完备。

```json
{
  "id": "hs_002",
  "version": 1,
  "latent_variable": "用户重跑实验的首要触发因素",
  "status": "frozen",
  "hypotheses": [
    {"id": "h1", "text": "结果波动异常", "p": 0.4},
    {"id": "h2", "text": "实验控制存在缺陷", "p": 0.4},
    {"id": "h3", "text": "外部反馈促使复查", "p": 0.2}
  ]
}
```

约束：

- 同一集合内 `sum(p) == 1`。
- EIG 计算期间不得增删假设。
- 用户选择“其他”且现有假设均不适用时，结束当前 episode，创建 `version + 1` 的新集合。
- 不跨版本直接比较原始熵。
- 可并存的知识规则不得放入同一个概率单纯形；它们应成为独立 Claim。

### 4.5 Question 与 likelihood

```json
{
  "id": "q_006",
  "text": "你当时首先检查的是哪一项？",
  "options": [
    {"id": "metric", "label": "指标波动"},
    {"id": "control", "label": "控制变量"},
    {"id": "feedback", "label": "导师或同伴反馈"},
    {"id": "other", "label": "其他"},
    {"id": "uncertain", "label": "不确定"}
  ]
}
```

条件概率按题目存储，不能挂在 Hypothesis 上：

```python
class QuestionLikelihood(BaseModel):
    question_id: str
    hypothesis_id: str
    probabilities: dict[str, float]
    source: Literal["llm_estimated", "empirical", "uniform_fallback"]
```

每个 `(question_id, hypothesis_id)` 下的选项概率之和必须为 1。LLM 可批量估计矩阵，但估计值、模型版本和原始响应必须记录，以便回放。

### 4.6 Claim 与证据边

```json
{
  "id": "c_004",
  "text": "当模型结果对随机种子稳定、但组间差异仍异常时，应先检查数据划分，再增加训练次数。",
  "type": "heuristic",
  "evidence_links": [
    {
      "evidence_id": "ev_001",
      "turn_id": "turn_03",
      "quote": "换了种子以后差距还在，大约百分之十二",
      "relation": "supports",
      "asserted_by": "user"
    }
  ],
  "boundaries": [
    {
      "text": "若数据划分已由独立脚本校验，优先检查实现差异",
      "status": "user_supported",
      "confirmed_by_turn_id": "turn_08"
    }
  ],
  "confidence": 0.65,
  "status": "user_confirmed"
}
```

规则：

- 没有任何可定位证据的内容只能保存为 `model_proposal`；基于模型已有知识的内容标为 `model_only`。
- 一个 Claim 可关联多条支持、反驳或限定证据。
- 外部事实型 Claim 需要来源支持；个人理解或目标型 Claim 需要用户确认。
- `confidence` 表示系统内部置信度，不代表结论在学术上为真。

### 4.7 Challenge

模型提出的边界或反例初始状态只能是 `untested`：

```json
{
  "id": "ch_003",
  "claim_id": "c_004",
  "text": "如果组间差异来自评估脚本而不是数据划分，这条规则是否仍适用？",
  "source": "model_generated_probe",
  "status": "untested",
  "confirmed_by_turn_id": null
}
```

用户裁决选项为：`成立`、`不成立`、`仅在某条件成立`、`没遇到过`、`无法判断`。只有用户提供具体经历或材料后，候选边界才能标为 `user_supported`。

## 5. EIG 与信念更新

设冻结假设集为 $H=\{h_1,\dots,h_n\}$，先验为 $p_i=P(h_i)$。候选问题 $q$ 的有限答案空间为 $A(q)$。

$$P(a\mid q)=\sum_i p_iP(a\mid h_i,q)$$

$$P(h_i\mid a,q)=\frac{p_iP(a\mid h_i,q)}{\sum_jp_jP(a\mid h_j,q)}$$

$$\operatorname{EIG}(q)=H(p)-\sum_{a\in A(q)}P(a\mid q)H(p\mid a,q)$$

其中 $H(p)=-\sum_i p_i\log_2p_i$。熵、EIG 和更新全部由 NumPy 计算。

实现约束：

- 对 likelihood 使用噪声下限 `0.02`，再归一化。
- 缺失的 likelihood 使用均匀分布并标记 `uniform_fallback`；该问题默认不参与选题，除非无其他候选。
- 用户明确选择某一选项时，使用该选项更新。
- 对自由文本回答，`answer_interpreter` 输出 `selected_option`、`confidence` 和支持片段；`confidence < 0.7` 时先澄清，不更新。
- `other` 触发假设集扩展流程；`uncertain` 不更新后验，并计入疲劳判断。
- 每轮保存预测 EIG、选中问题、实际答案、更新前后分布和集合版本。

初始参数：

| 参数 | 初值 |
|---|---:|
| `min_eig_to_ask` | 0.03 bit |
| `max_proposal_attempts` | 2 |
| `turn_budget` | 12 |
| `max_challenges` | 2 |
| `answer_mapping_threshold` | 0.70 |

这些值是实验起点，不是理论常数，应通过试用数据调整。

## 6. 停止与负担控制

任一条件满足即进入 `finalize`：

```text
user_requested_stop
OR turns_used >= turn_budget
OR fast 模式已经满足 desired_output
OR proposal_attempts >= max_proposal_attempts 且没有更高价值候选
OR 连续两轮没有新增证据、候选差异、理解变化或可执行步骤
OR 继续检索或追问的预期价值低于成本
OR 当前目标已由用户确认完成
```

EIG 只控制离散澄清题，不作为整个问答的统一停止指标。停止时必须记录 `stop_reason`。若由系统阈值自动停止，可允许用户继续；若用户明确要求停止，则直接结束。

## 7. LangGraph 中断与持久化

`clarify` 和 `learn_check` 节点需要用户输入时使用动态中断：

```python
answer = interrupt({
    "kind": "question",
    "question_id": state["pending_question"].id,
    "text": state["pending_question"].text,
    "options": state["pending_question"].options,
})
```

恢复调用：

```python
graph.invoke(
    Command(resume={"question_id": question_id, "answer": answer}),
    config={"configurable": {"thread_id": session_id}},
)
```

工程要求：

- 编译图时配置持久化 checkpointer；生产环境使用 SQLite checkpointer。
- `pending_question` 在中断前进入 state，但数据库业务写入尽量放在恢复后。
- turns、answers 和 evidence links 使用稳定 ID 与唯一约束，保证节点重放时幂等。
- resume 中的 `question_id` 必须与 pending question 一致，拒绝过期或重复回答。
- 每个 session 同时只允许一个待回答问题。
- 检索、发散和评分结果在中断前落入 checkpoint，恢复时不得重复产生不同候选。

## 8. 模型调用契约

所有模型调用使用结构化输出并做 Pydantic 校验。失败时最多重试两次，仍失败则使用安全降级或结束当前步骤。

### 8.1 全局约束

```text
你是知识激发智能体。目标是提高回答对用户理解、选择和行动的实际价值。
1. 先识别任务目标与价值标准，避免复述题意和填充背景常识。
2. 区分来源事实、合理推断、模型候选和未知项。
3. 关键事实优先引用原始资料；无法核验时明确标注。
4. 复杂任务先生成实质不同的候选，再比较、反驳和综合。
5. 一次最多向用户提出一个最有价值的问题；若无需澄清则直接推进。
6. 输出机制、关键差异、边界和可执行验证，不披露或依赖隐藏思维链。
7. 引用、数字、版本、反例和不确定性不得在压缩中删除。
```

### 8.2 任务框定

输出 `task_type`、`depth`、`desired_output`、`value_criteria`、`missing_information` 和 `need_fresh_sources`。`missing_information` 中每一项必须说明它可能如何改变答案，否则删除。

### 8.3 候选问题

输入：当前目标、证据、冻结假设集、最近问题。  
输出：3～5 个问题，每题包含 `id`、`text`、2～5 个实质选项，以及固定的 `other`、`uncertain`。因此总答案数为 4～7 个；EIG 对完整答案空间计算。问题不得与最近问题语义重复。

### 8.4 likelihood 批量估计

一次调用返回所有 `(hypothesis, question)` 的答案分布。代码校验每行非负且和为 1；无效行改为均匀分布并记录降级原因。模型不得计算 EIG。

### 8.5 候选生成与批判

候选生成器只负责制造差异，不负责宣布最佳答案。批判器逐项输出依据、假设、失败模式和最低成本验证。两个调用使用不同提示契约，避免生成器在发散时自我审查而退回通识答案。

### 8.6 Claim 草拟

模型基于 `EvidenceItem` 和候选综合草拟 Claim。事实判断必须引用 evidence ID；推断必须列出前提；没有来源的内容标为 `model_only` 或 `model_proposal`。

### 8.7 边界探针

模型输出具体失效条件，但不得填写“已验证”或虚构证据。所有候选统一标为 `untested`，等待用户裁决。

## 9. 存储与导出

目录建议：

```text
knowledge_elicitation/
├── pyproject.toml
├── src/kel/
│   ├── graph.py
│   ├── state.py
│   ├── models.py
│   ├── nodes/
│   │   ├── task_frame.py
│   │   ├── depth_router.py
│   │   ├── clarify.py
│   │   ├── source.py
│   │   ├── diverge.py
│   │   ├── score.py
│   │   ├── eig.py
│   │   ├── interpret.py
│   │   ├── belief.py
│   │   ├── challenge.py
│   │   ├── synthesize.py
│   │   ├── learn_check.py
│   │   └── finalize.py
│   ├── prompts/
│   ├── store.py
│   ├── eval/
│   └── api.py
└── tests/
```

SQLite 表：`sessions`、`turns`、`task_frames`、`evidence_items`、`evidence_spans`、`hypothesis_sets`、`hypotheses`、`questions`、`likelihoods`、`candidates`、`candidate_scores`、`claims`、`claim_evidence_links`、`challenges`、`entropy_records`。

导出包含：

- `summary.md`：最终结论、学习产物或方案，以及证据、边界和未知项。
- `graph.json`：`EvidenceItem → Claim → Boundary` 图结构。
- `replay.jsonl`：逐轮输入、来源、候选、评分、模型与 prompt 版本、EIG 和状态变化。

## 10. 评测

### 10.1 自动指标

| 指标 | 定义 | MVP 目标 |
|---|---|---:|
| 关键事实来源率 | 可外部核验的关键事实中含有效来源定位的比例 | ≥ 90% |
| 来源等级准确率 | primary、secondary、model_only 未被混淆的比例 | 100% |
| 候选差异率 | deep 模式候选中非同义改写的比例 | ≥ 80% |
| 通识冗余率 | 删除后不影响理解、选择或行动的内容占比 | 低于直答基线 |
| 行动信息覆盖率 | 适用任务中含步骤、成功信号或验证方法的比例 | ≥ 90% |
| 问题重复率 | 相邻问题语义相似度超过阈值的比例 | < 10% |
| 中断恢复成功率 | 可在同一 pending question 恢复且无重复写入 | 100% |
| 边界覆盖率 | deep 模式关键 Claim 含前提或失效条件的比例 | ≥ 80% |
| 退出尊重率 | 用户请求停止后不再提出新问题的比例 | 100% |

自动指标只能验证结构与流程，不能证明 Claim 的真实性或教学价值。

### 10.2 人工评估

采用盲评，让评审比较本智能体回答与同模型普通直答。以 1～5 分评价：

- 是否包含直答缺少、且真正有用的新信息；
- 是否建立了机制性理解而非术语罗列；
- 证据是否可靠、可定位，并与推断明确区分；
- 候选是否有实质差异，最终取舍是否有依据；
- 是否产生可执行步骤、练习、验证或决策；
- 内容密度与交互负担是否可接受。

“信息量”不能直接用 token 数或术语密度替代。MVP 使用三个可操作代理量：新增且非冗余的命题数、能区分候选的关键差异数、会改变用户下一步行动的内容比例。

EIG 校准作为研究指标：只在假设集版本不变的 episode 内比较预测 EIG 与实际熵下降。样本不足时只报告散点和误差，不设置未经实测的相关系数硬门槛。

### 10.3 MVP 基线

保留三个必要基线，并使用相同基础模型和相同资料权限：

1. 模型普通单次直答。
2. 只加入“请深入、详细回答”的增强提示。
3. 完整知识激发流程。

针对离散澄清问题，再单独比较 EIG 选题与随机选题。针对发散任务，比较单候选与“生成—批判—综合”，不能混成一个总分。

## 11. 里程碑

| 里程碑 | 内容 | 验收 |
|---|---|---|
| M1 | task frame + fast/deep 路由 | 简单问题直接答，复杂问题生成明确价值标准 |
| M2 | 来源分级与 EvidenceItem | 官方资料、用户材料和模型记忆不会混淆 |
| M3 | 发散—批判—综合闭环 | 候选有实质差异，取舍有证据和失败模式 |
| M4 | EIG 澄清模块 | 合成分布下排序、更新正确；仅在适用场景启用 |
| M5 | interrupt + resume | 会话可中断恢复，无重复 turn 或候选漂移 |
| M6 | 学习检查与产物导出 | 可生成解释、练习、实现步骤或决策摘要 |
| M7 | 小规模对照评测 | 相对普通直答提高信息价值，且成本与负担可接受 |

## 12. 交付判据

一次知识激发成功不要求固定输出形态，但至少满足：

1. 回答针对用户真实目标，而不是泛化介绍；
2. 关键内容包含机制、关键差异、边界或验证方法中的至少两项；
3. 事实、推断、模型候选和未知项可以区分，重要来源可以定位；
4. 复杂问题考虑过实质不同的候选，没有在第一个常见答案上过早收敛；
5. 用户得到一个可检查的结果：更准确的心智模型、练习反馈、可运行实现、决策依据、研究假设或下一步行动。

更长、更像专家或术语更多都不算成功。真正的标准是：相较普通直答，是否增加了能改变理解、选择或行动的可靠信息。
